document.addEventListener("DOMContentLoaded", () => {
    document.getElementById("search-button").addEventListener("click", () => {
        alert("Busca ainda não implementada.");
    });

    document.getElementById("show-atms").addEventListener("click", () => {
        alert("Exibir apenas ATMs (ainda não implementado).");
    });

    document.getElementById("show-stores").addEventListener("click", () => {
        alert("Exibir apenas Lojas (ainda não implementado).");
    });

    document.getElementById("show-favorites").addEventListener("click", () => {
        alert("Exibir Favoritos (ainda não implementado).");
    });
});
